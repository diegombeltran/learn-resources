image: node:slim

before_script:
  - npm install -g static-marks

pages:
  stage: deploy
  script:
  - /usr/local/bin/static-marks files/* -o index.html
  - mv index.html public/
  artifacts:
    paths:
    - public
  only:
  - master
